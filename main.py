import mod

# result = help(mod) / modül hakkında bilgiler alırız
# result = help(mod.func) / modül içindeki func isimli fonksiyonun bilgilerini alırız
# result = mod.number / modül içindeki number değişkenine erişim sağlar
# result = mod.numbers / modül içindeki numbers listesine erişim sağlar
# result = mod.person["name"] / modül içindeki person dic. listesinin name bilgisine erişim sağlar
# result = mod.person["age"] / modül içindeki person dic. listesinin name bilgsine erişim sağlar
# result = mod.func(x) / modül içindeki fonksiyonu çalıştırır. 
# p = mod.Person() / modül içindeki classı p'ye atar
# p.speak() / atanan p aracılığıyla person classındaki speak methoduna erişim sağlar

# print(result)